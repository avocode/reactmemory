{
  "name": "reactmemory",
  "version": "0.0.1",
  "author": "jankuca",
  "licence": "MIT",
  "main": "dist/react-memory",
  "scripts": {
    "flow": "flow",
    "prepublish": "rm -rf dist && babel src -d dist && cp -R types/* dist/",
    "prettier": "prettier -c src types"
  },
  "dependencies": {
    "react-reconciler": "0.25.1"
  },
  "devDependencies": {
    "@babel/cli": "7.10.3",
    "@babel/core": "7.10.3",
    "@babel/plugin-transform-flow-strip-types": "7.10.1",
    "@babel/preset-env": "7.10.3",
    "flow-bin": "0.128.0",
    "prettier": "2.0.5"
  }
}
